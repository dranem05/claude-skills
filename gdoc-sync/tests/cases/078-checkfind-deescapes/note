The markdown export carries backslash escapes the live document body does not, so a find string taken from the doc legitimately matches zero times raw and once de-escaped; dropping that fallback refuses valid pushes.
