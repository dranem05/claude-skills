KNOWN LIMITATION, pinned deliberately. A partial export that drops a tab duplicating retained content makes checkfind certify a string as unique when it is not, and the push then over-matches live. This is the corruption the consumer skill records as having already happened. checkfind cannot see it; only the one-sidedness guard flags the run at all. If a future change makes checkfind catch this, this case must be updated, not deleted.
