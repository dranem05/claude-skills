R=$(bash "$GD/gdsync" init --local local.md --label t021 --mode pull-only --export drive-src.md | sed -n 's/^RUN=//p')
bash "$GD/gdsync" diff --run "$R"
echo "MAIN_DIFF_BYTES=$(wc -c < "$R/diff.txt" | tr -d ' ')"
