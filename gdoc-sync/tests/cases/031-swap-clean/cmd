R=$(bash "$GD/gdsync" init --local local.md --label t031 --mode pull-only --export drive-src.md | sed -n 's/^RUN=//p')
bash "$GD/gdsync" swap --run "$R" --start 'START_X' --end 'END_X'
grep -q 'NEW body from drive' local.md && echo APPLIED
grep -q 'tail' local.md && echo TAIL_INTACT
